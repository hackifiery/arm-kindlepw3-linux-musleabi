# arm-kindlepw3-linux-musleabi
configs and patches to get a crosstool-ng toolchain for the kindle paperwhite 3 working
## How to build
install crosstool-ng and it's dependencies, clone this repo, then just run `ct-ng build`. It should install the toolchain (arm-kindlepw3-linux-musleabi) in ~/x-tools.
